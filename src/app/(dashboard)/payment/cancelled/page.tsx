import PaymentCancelledContainer from '@/features/dashboard/payment/payment-cancelled-container';

const CancelledPage = () => {
  return <PaymentCancelledContainer />;
};

export default CancelledPage;
